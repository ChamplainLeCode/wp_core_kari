import 'package:flutter_test/flutter_test.dart';

/*
 * @Author Champlain Marius Bakop
 * @Email champlainmarius20@gmail.com
 * @Github ChamplainLeCode
 * 
 */
void main() {
  TestWidgetsFlutterBinding.ensureInitialized();

  setUp(() async {});
  testWidgets('Payment Module loading', (WidgetTester tester) async {
    // KareeMaterialApp.type = KareeApplicationType.module;
    // await PaymentModule().initialize();
    // var app = MyKareeApp();
    // Build our app and trigger a frame.
    // await tester.pumpWidget(app);
    // await tester.pump();
    // print(find.byType(MyKareeApp, skipOffstage: false));
    // var btn = find.widgetWithText(TextButton, 'Get Started');
    // // var promoBtn = find.widgetWithText(ElevatedButton, 'View Promotion');
    // // print(promoBtn);
    // await tester.tap(btn);
    // await tester.pump(Duration.zero);
    // tester.allElements.map((e) => print(e));
    // // .where((element) => element.runtimeType == PaymentDashboardScreen);
    // // print(e);
    // var wid = find.byType(PaymentDashboardScreen);
    // // print(wid);
    // // expect(wid, findsOneWidget);
    expect(1, 1);
  });
}
